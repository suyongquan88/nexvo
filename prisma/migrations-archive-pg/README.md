# Archived PostgreSQL migrations

These migrations targeted PostgreSQL before Sprint 2 switched to SQLite.

Do not run against the SQLite database. Kept for reference when migrating to PostgreSQL in a later phase.
